<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_width="@dimen/landscape_image_width"
        android:layout_height="@dimen/landscape_image_height"
        app:srcCompat="@drawable/poster"
        android:id="@+id/MovieImageid"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <TextView
        tools:text="Capt. America"
        android:textSize="@dimen/header_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/MovieTitleId"
        android:textColor="@color/colorWhite"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/landscape_title_margin_top"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/MovieImageid" />

    <TextView
        tools:text="When a wounded Christian Grey tries to entice a cautious Ana Steele back into his life, she demands a new arrangement before she will give him another chance. As the two begin to build trust and find stability, shadowy figures from Christian’s past start to circle the couple, determined to destroy their hopes for a future together."
        android:layout_width="@dimen/landscape_description_width"
        android:layout_height="@dimen/landscape_description_height"
        android:textSize="@dimen/desc_size"
        android:textColor="@color/colorWhite"
        android:textStyle="italic"
        android:id="@+id/MovieDescId"
        android:layout_below="@+id/MovieTitleId"
        android:layout_toEndOf="@+id/MovieImageid" />

</RelativeLayout>